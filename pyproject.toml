[project]
name = "robust-input"
version = "0.9.0"
description = "A very comprehensive terminal input module with no external dependencies"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = [
    "black==25.1.0",
]
